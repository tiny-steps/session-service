apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-session-service-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ts-session-service
  template:
    metadata:
      labels:
        app: ts-session-service
    spec:
      containers:
        - name: ts-session-service
          image: subhankarm2312/ts-session-service:f38aacb-20250810081605 # This will be updated by the pipeline
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-Xms128m -Xmx256m"
          resources:
            requests:
              memory: "128Mi" # Request 256 MiB of memory
              cpu: "100m"      # Request 0.25 of a CPU core
            limits:
              memory: "512Mi" # Limit the pod to a max of 512 MiB
              cpu: "500m"      # Limit the pod to a max of 0.5 of a CPU core
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: ts-session-service-config
            - secretRef:
                name: ts-session-db-credentials
