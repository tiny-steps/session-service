apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-session-service-deployment
  labels:
    app: ts-session-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ts-session-service
  template:
    metadata:
      labels:
        app: ts-session-service
    spec:
      containers:
        - name: ts-session-service
          image: subhankarm2312/ts-session-service # CORRECTED: Placeholder name now matches ci.yml
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-Xms128m -Xmx256m"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          ports:
            - containerPort: 8087
          envFrom:
            - configMapRef:
                name: ts-session-service-config
            - secretRef:
                name: ts-session-service-secret # CORRECTED: Name now matches secret.yml
